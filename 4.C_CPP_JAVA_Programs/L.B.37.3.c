/*3. Write a program which accept string from
user and reverse each word in place.
Input :   “Marvellous Multi OS”
Output :   “suollevraM itluM SO”   */
#include<stdio.h>
int main()
{
    int ans;
    char srr[300]="Marvellous Multi OS Infosystems";
   funcall(srr);
    return 0;
}
int funcall(char *str)
{
    int i,j,k=0,x,len;

}
